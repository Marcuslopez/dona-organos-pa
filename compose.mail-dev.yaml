name: dona-organos-pa-mail-dev

services:
  smtp4dev:
    image: rnwood/smtp4dev:v3
    restart: unless-stopped
    ports:
      - "127.0.0.1:2525:25"
      - "127.0.0.1:8080:80"
    volumes:
      - smtp4dev-data:/smtp4dev

volumes:
  smtp4dev-data:
